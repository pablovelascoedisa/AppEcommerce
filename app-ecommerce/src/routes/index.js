import { createRouter, createWebHistory } from "@ionic/react-router";

const routes = [
  // routes go here
];

const router = createRouter({
  history: createWebHistory(),
  routes,
});

export default router;
